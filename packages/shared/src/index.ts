/**
 * Beacon Shared Types and Models
 * Exports common types used across the extension.
 */

export type {
  BoundingBox,
  InteractionType,
  Interaction,
  UIElementType,
  UIElement,
  ViewportContext,
  PageContext,
  VisibilityState,
  HighlightInstruction,
  HighlightStyle,
  GuideRequest,
  GuideResponse,
} from './types'
